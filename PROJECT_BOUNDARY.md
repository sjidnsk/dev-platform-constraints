# 项目边界：dev-platform-constraints

## 定位

`dev-platform-constraints` 是月面巡视探索研究原型的建模底座，负责把环境、平台能力、地图可信度和通行约束表达成可被决策层和执行层消费的数据结构。

它回答的问题是：当前世界如何表达，哪些区域可通行，地图信息是否可靠，候选观测目标的基础收益和风险如何估计。

## 负责范围

- 多层栅格地图：`GridMap`、核心图层契约、元数据、单位和有效性校验。
- 地形派生：坡度、崎岖度等环境特征。
- 平台参数：平台能力配置、来源标记和保守校验。
- 硬约束与代价图：`passable_mask`、原因位图、`cost`、`traversability`。
- 可信度建模：`c_resolution`、`c_observation`、`c_recency`、`c_consistency`、贝叶斯状态后验和观测更新。
- 候选目标基础能力：从低可信、高价值、风险和可达性等地图层生成离散候选，并给出基础效用评分。
- 可复现实验底座：最小闭环脚本、可信度权重消融、数据契约报告和静态可视化。

## 不负责范围

- 不实现完整自主探索主循环，例如“选目标 -> 执行 -> 观测 -> 重规划”的长期调度。
- 不作为 `model-explorer` 的替代，不承载探索策略、重规划策略或任务级状态机。
- 不实现 Hybrid A*、IRIS、GCS、Ackermann 轨迹优化或车辆动力学执行层。
- 不把低可信区域直接改成不可通行；低可信只通过风险代价影响规划。
- 不把 `sensor_range`、`sensor_fov`、`min_turning_radius` 写成公开事实；这些字段仍按 `assumed` 仿真参数处理。

## 对外接口边界

向 `model-explorer` 提供：

- `GridMap`
- `confidence`、`value`、`cost`、`traversability`
- `passable_mask` 和硬约束原因层
- 候选目标列表、基础效用项、可达性和路径代价
- 观测更新后的可信度报告

接收 `model-explorer` 的请求：

- 生成或更新地图/约束/代价图
- 评估候选目标
- 模拟或应用局部观测更新
- 输出报告与实验指标

## 当前阶段原则

本目录可以继续增强“底座能力”和“可解释指标”，但不继续上探到完整决策闭环。若需要实现探索循环，应在 `model-explorer` 中编排，并复用本目录 API。
